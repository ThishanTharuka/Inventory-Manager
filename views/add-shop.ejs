<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/nav.css">
    <link rel="stylesheet" href="/css/add-page.css">
    <title>
        <%= title %>
    </title>
    <link rel="icon" href="/images/dealer.svg" type="image/x-icon">
</head>

<body>
    <!-- Include the navigation partial -->
    <%- include('./partials/nav.ejs') %>
        <div id="container">
            <form action="<%= mode === 'add' ? '/add-shop' : '/update-shop' %>" method="post">
                <div id="input-fields">
                    <label for="Shop Name">Dealer Name</label>
                    <input type="text" id="shop_name" name="shop_name"
                        value="<%= mode === 'edit' ? shop.shop_name : '' %>" required>
                </div>
                <div id="input-fields">
                    <label for="Contact_no">Contact No.</label>
                    <input type="number" id="contact_no" name="contact_no"
                        value="<%= mode === 'edit' ? shop.contact_no : '' %>">
                </div>
                <div id="input-fields">
                    <label for="Location">City</label>
                    <input type="text" id="location" name="location" value="<%= mode === 'edit' ? shop.location : '' %>"
                        required>
                </div>
                <div id="input-fields">
                    <label for="Address">Address</label>
                    <input type="text" id="address" name="address"
                        value="<%= mode === 'edit' ? shop.Address : '' %>" required>
                </div>
                <!-- Hidden input field for shop_id -->
                <input type="hidden" id="shop_id" name="shop_id" value="<%= mode === 'edit' ? shop.shop_id : '' %>">
                <div id="add-item-btn">
                    <button type="submit">
                        <%= mode==='add' ? 'Add Shop' : 'Update Shop' %>
                    </button>
                </div>
            </form>
        </div>
</body>

</html>
